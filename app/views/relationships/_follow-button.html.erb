    <% if current_user.following?(user) %>
        <%= link_to "フォロー解除", unfollow_path(user), method: :post, class:"btn btn-info", remote: true %>
    <% else %>
        <%= link_to " フォローする", follow_path(user), method: :post, class:"btn btn-info", remote: true %>
    <% end %>